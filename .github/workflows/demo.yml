name: Demo CI/CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "🚀 Starting Deployment..."

            # Install Python3 and proper venv package
            echo "📦 Installing Python3 and virtual environment support..."
            sudo apt update
            sudo apt install -y python3 python3-full python3-venv

            # Create deployment folder
            sudo mkdir -p /opt/demo-app
            sudo chown ubuntu:ubuntu /opt/demo-app
            cd /opt/demo-app

            echo "📥 Fetching latest code..."
            if [ -d .git ]; then
              git pull origin main
            else
              git clone https://github.com/vtricksshiva/cicd.git .
            fi

            echo "📦 Setting up Python virtual environment..."
            # Remove old venv if exists and create new one
            rm -rf venv
            python3 -m venv venv --without-pip
            # Install pip manually if needed
            curl -sS https://bootstrap.pypa.io/get-pip.py | venv/bin/python

            echo "📦 Installing Python requirements in virtual environment..."
            venv/bin/pip install --upgrade pip
            venv/bin/pip install -r requirements.txt

            echo "🛑 Stopping old server..."
            # Kill any existing processes
            pkill -f "python3 app.py" || true
            pkill -f "venv/bin/python app.py" || true
            sleep 2

            echo "🚀 Starting Flask App..."
            # Start from virtual environment
            cd /opt/demo-app
            nohup venv/bin/python app.py > app.log 2>&1 &
            echo $! > app.pid

            echo "✅ Verifying deployment..."
            sleep 5
            if ps -p $(cat app.pid 2>/dev/null) > /dev/null 2>&1; then
              echo "🎉 Deployment completed successfully!"
              echo "📊 Process info:"
              ps -p $(cat app.pid)
              echo "🌐 Checking if app is responsive..."
              sleep 3
              curl -f http://localhost:5000/ || echo "App might not be fully started yet"
            else
              echo "❌ Deployment failed - process not running"
              echo "📋 Checking app.log for errors:"
              tail -20 app.log || echo "No log file found"
              exit 1
            fi
